const API_BASE=process.env.NEXT_PUBLIC_API_URL||"http://localhost:4000/api";
export class ApiError extends Error{status:number;errors?:unknown;constructor(message:string,status:number,errors?:unknown){super(message);this.status=status;this.errors=errors}}
function validationDetails(errors:unknown){if(!errors||typeof errors!=="object")return "";const fieldErrors=(errors as {fieldErrors?:Record<string,unknown>}).fieldErrors;if(!fieldErrors)return "";return Object.entries(fieldErrors).flatMap(([field,messages])=>Array.isArray(messages)?messages.map(message=>`${field}: ${String(message)}`):[]).join("; ")}
export async function api<T>(path:string,init:RequestInit={}):Promise<T>{const headers=new Headers(init.headers);if(init.body&&!(init.body instanceof FormData))headers.set("Content-Type","application/json");const response=await fetch(`${API_BASE}${path}`,{...init,headers,credentials:"include"});let payload:unknown=null;try{payload=await response.json()}catch{}const body=payload&&typeof payload==="object"?payload as {message?:string;errors?:unknown;data?:unknown}:null;if(!response.ok){const message=body?.message||`Request failed (${response.status})`;const details=validationDetails(body?.errors);throw new ApiError(details?`${message}: ${details}`:message,response.status,body?.errors)}return (body&&"data" in body?body.data:payload) as T}
export async function uploadImage(file:File){const body=new FormData();body.append("file",file);return api<{url:string}>("/admin/upload",{method:"POST",body})}
