import { notFound } from "next/navigation";import { PublicShell } from "../../components/PublicShell";import { publicApi } from "../../lib/public-api";
export default async function BlogPostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=await publicApi<any>(`/blog/${slug}`);if(!post)notFound();return <PublicShell><article className="article-page"><span className="eyebrow">{post.category?.name}</span><h1>{post.title}</h1><p className="article-meta">{new Date(post.publishedAt).toLocaleDateString()} · {post.readingTime} min read</p>{post.coverImage&&<img src={post.coverImage} alt=""/>}<div>{String(post.content).split("\n").map((x:string,i:number)=><p key={i}>{x}</p>)}</div></article></PublicShell>}
