import { notFound } from "next/navigation";import { PublicShell } from "../../components/PublicShell";import { ProductCard } from "../../components/HomepageRenderer";import { publicApi } from "../../lib/public-api";
export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=await publicApi<any>(`/categories/${slug}`);if(!category)notFound();const products=category.products.map((p:any)=>({...p,image:p.images?.[0]?.url}));return <PublicShell><section className="listing-page"><span className="eyebrow">Shop by category</span><h1>{category.name}</h1><p>{category.description}</p><div className="product-grid">{products.map((p:any)=><ProductCard key={p.id} product={p}/>)}</div></section></PublicShell>}
