import Link from "next/link";
import type { FooterData, SiteSettings } from "../lib/types";
export function SiteFooter({data,settings}:{data:FooterData;settings:SiteSettings}){const name=settings.websiteName||"Bargain MOM";const logo=settings.logo||"/brand/bargain-mom-logo.png";return <footer><div className="footer-main"><div className="footer-brand"><Link className="brand brand-logo footer-logo" href="/" aria-label={`${name} home`}><img src={logo} alt={name}/></Link><p>{data.description || "A modern affiliate publishing platform for thoughtful recommendations."}</p></div>{data.columns?.map(col=><div key={col.id}><h4>{col.title}</h4>{col.links.map(link=><Link key={link.id} href={link.url}>{link.label}</Link>)}</div>)}</div><div className="footer-bottom"><span>{data.copyrightText || `© ${new Date().getFullYear()} ${name}`}</span><span>Built for better decisions.</span></div></footer>}
