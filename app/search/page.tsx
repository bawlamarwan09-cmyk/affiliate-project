import { PublicShell } from "../components/PublicShell";import { ProductCard } from "../components/HomepageRenderer";import { publicApi } from "../lib/public-api";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q||"";const products=q?await publicApi<any[]>(`/products?q=${encodeURIComponent(q)}`):[];return <PublicShell><section className="listing-page"><span className="eyebrow">Search</span><h1>{q?`Results for “${q}”`:"Find your next deal"}</h1><form className="hero-search" action="/search"><input name="q" defaultValue={q} placeholder="Search products, brands or categories"/><button>Search</button></form>{products?.length?<div className="product-grid">{products.map((p:any)=><ProductCard key={p.id} product={{...p,image:p.images?.[0]?.url}}/>)}</div>:q?<p>No matching products found.</p>:null}</section></PublicShell>}
